extends layout

block content
  h2=title
  ul
    each category in categories
      li
        a(href=category.url)=category.name
        span.mono
          i=`(${category.animalCount} species in stock)`
    else
      li No categories available
