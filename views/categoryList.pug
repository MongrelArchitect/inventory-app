extends layout

block content
  h1=title
  ul
    each category in categories
      li
        a(href=category.url)=category.name
        span=`(${category.animalCount} species in stock)`
    else
      li
