extends layout

block content
  h1=title
  ul
    each animal in animals
      li
        a(href=animal.url)=animal.commonName
    else
      li='no animals found'
  div=`${getTotalAnimals(animals).toLocaleString()} in stock`
  div=`${formatPrice(getTotalValue(animals))} total`
